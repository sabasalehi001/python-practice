# PrimePaladin

PrimePaladin is a simple command-line tool to find prime numbers within a specified range.

## Usage

Run `python main.py` and follow the prompts to enter the start and end of the range you want to search for primes in.

## Example

```
python main.py
```
