# AureusArbiter

A simple command-line currency exchange calculator.

## Usage

Run `python main.py` and follow the prompts to enter the source currency, target currency, and amount to convert.  The program will output the converted amount.

## Example

```
python main.py
```
