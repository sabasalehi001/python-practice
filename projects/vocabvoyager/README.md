# VocabVoyager

A simple command-line flashcard quiz for building vocabulary.

## Usage

Run `python main.py` and follow the prompts to add new flashcards or start a quiz.

## Features

*   Add new word-definition pairs to the flashcard set.
*   Take a quiz on the current flashcard set.
*   Displays score at the end of the quiz.
