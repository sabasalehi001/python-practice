# LexiLens: A Simple Regex Tester

LexiLens is a command-line tool that allows you to test regular expressions against a given text string.

## Usage

Run `python main.py` and follow the prompts to enter your regular expression and text.

## Example

```
python main.py
```

Then enter the regular expression and the text to test against when prompted.
