# CipherCaddy

A simple command-line tool for basic text encryption and decryption using a Caesar cipher.

## Usage

Run `python main.py` and follow the prompts to encrypt or decrypt text using a specified shift value.

## Example

```
python main.py
```

## Notes

This is a very basic encryption method and should not be used for sensitive data.
